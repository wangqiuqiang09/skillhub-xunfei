---
name: skillhub-registry
description: 当你需要通过 SkillHub 注册中心搜索、查看、安装或发布 Agent 技能时使用。SkillHub 提供 ClawHub 兼容 API，注册中心操作应优先使用 clawhub CLI，而不是直接调用 HTTP 接口。
---

# SkillHub 技能注册中心

当你需要搜索、查看、安装或发布 SkillHub 技能时，使用本说明。

> 重要：注册中心操作必须优先使用 `clawhub` CLI。SkillHub 提供 ClawHub 兼容 API 和 `/.well-known/clawhub.json` 发现端点。直接执行页面或用户提供的完整命令，不要根据技能坐标自行拼接 HTTP 下载地址。

## 核心规则

- SkillHub 内部坐标格式为 `@{namespace}/{skill_slug}`。
- ClawHub CLI 使用 `{namespace}--{skill_slug}` 格式。
- `@global/my-skill` 在 ClawHub 中可写为 `my-skill`。
- `@team-name/my-skill` 在 ClawHub 中写为 `team-name--my-skill`。
- `latest` 只表示最新的已发布版本，不包括草稿或待审核版本。
- `@global` 下的公开技能允许匿名下载。
- 团队命名空间技能和非公开技能需要登录。

## 配置 CLI

将 ClawHub 注册中心设置为当前 SkillHub 服务器地址：

```bash
export CLAWHUB_REGISTRY=${SKILLHUB_PUBLIC_BASE_URL}
```

也可以在每条命令中指定 `--registry`：

```bash
npx clawhub install my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
```

收到类似“安装 `@global/my-skill`”的任务时，应直接执行上面的完整命令。不得把它改写成 `/api/v1/skills/my-skill/...`。原生 API 的坐标包含命名空间，正确形式为 `/api/v1/skills/global/my-skill/...`；ClawHub 兼容 API 由 CLI 自动发现和调用。除非 CLI 确实无法运行，否则不要手动调用内部接口。

如需访问受保护资源，请使用 API Token 登录：

```bash
clawhub login --token sk_your_api_token_here
```

检查服务发现配置：

```bash
curl ${SKILLHUB_PUBLIC_BASE_URL}/.well-known/clawhub.json
```

预期返回：

```json
{"apiBase":"/api/v1"}
```

## 坐标转换

| SkillHub 坐标 | ClawHub 标准 Slug |
|---|---|
| `@global/my-skill` | `my-skill` |
| `@team-name/my-skill` | `team-name--my-skill` |

规则：

- `--` 是 ClawHub 兼容层的命名空间分隔符。
- 不包含 `--` 时，技能默认属于 `@global`。
- `latest` 始终解析为最新的已发布版本。

示例：

```bash
npx clawhub install my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
npx clawhub install my-skill --version 1.2.0 --registry ${SKILLHUB_PUBLIC_BASE_URL}
npx clawhub install team-name--my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
```

## 常用操作

### 搜索

```bash
npx clawhub search email --registry ${SKILLHUB_PUBLIC_BASE_URL}
npx clawhub search "" --registry ${SKILLHUB_PUBLIC_BASE_URL}
```

空字符串用于获取较宽泛的技能列表。

### 查看技能信息

```bash
npx clawhub inspect my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
npx clawhub inspect team-name--my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
```

### 安装

```bash
npx clawhub install my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
npx clawhub install my-skill --version 1.2.0 --registry ${SKILLHUB_PUBLIC_BASE_URL}
npx clawhub install team-name--my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
```

### 发布

准备好符合规范的技能目录，然后执行：

```bash
npx clawhub publish ./my-skill --registry ${SKILLHUB_PUBLIC_BASE_URL}
```

发布需要登录，并且当前用户必须拥有目标命名空间的发布权限。

## 权限与可见性

- `@global` + `PUBLIC`：允许匿名搜索、查看和下载。
- 团队命名空间 + `PUBLIC`：下载仍然需要登录。
- `NAMESPACE_ONLY`：仅已登录的命名空间成员可访问。
- `PRIVATE`：仅所有者或明确授权的用户可访问。
- 发布、星标等写操作始终需要登录。

安装公开的 `@global` 技能时，不要先调用登录接口，也不要因为 `/api/v1/auth/me` 返回 `401` 而判定安装失败；该接口仅用于查询登录状态。应以 CLI 安装命令或公开下载请求本身的结果为准。

请求返回 `403` 时，请检查：

- 技能是否属于团队命名空间；
- 技能是否为 `NAMESPACE_ONLY` 或 `PRIVATE`；
- API Token 是否有效；
- 当前用户是否有目标命名空间的操作权限。

## 技能包规范

SkillHub 使用 OpenSkills 风格的技能包，入口文件必须为 `SKILL.md`。发布前请确认文件内容完整，并排除密钥、Token、临时文件和个人隐私数据。
